import { mainLocale } from '#model/{{ gameId }}/schema/basic';

export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green',
      neutral: 'slate',
    },
  },

  gameId: '{{ gameId }}',

  appIcon: 'i:{{ gameId }}-logo',

  locales: mainLocale.options,
});
