# 炉石卡牌可见范围与衍生物关联展示实现计划

## Todo

- [x] 确认主卡牌过滤规则，特别是收藏英雄外观是否默认隐藏。
- [x] 更新服务端搜索逻辑，只返回默认可见主卡牌。
- [x] 更新导入脚本，生成 `herald_token`、升级牌和集合关联关系。
- [x] 清理炉石文本标记和数字占位符。
- [x] 重新执行炉石导入并校验 `card_relations`。
- [x] 更新详情页关联展示分组，突出英雄技能和兆示衍生物。
- [x] 用浏览器验证搜索结果和典型详情页。
- [x] 为泰坦卡牌生成并展示 `titan_ability` 技能关系。
- [x] 为缺失的金卡资源增加普通卡图兜底。
- [x] 清理旧版 `version=1` 导入残留，避免英文历史版本混入错误实体。
- [x] 为疫病相关卡牌生成并展示三张疫病牌关系。

## 实施步骤

1. 在服务端增加统一的炉石默认可见过滤条件。
2. 将搜索接口套用该过滤条件，确保 token、衍生物、酒馆战棋牌和系统实体不进入普通搜索结果。
3. 在导入脚本中识别兆示主卡牌和兆示衍生物。
4. 按职业匹配生成 `herald_token` 关系，中立主卡牌关联全部兆示衍生物。
5. 重新运行导入脚本，检查英雄技能和兆示衍生物关系数量。
6. 调整详情页关联展示，把英雄技能和兆示衍生物放到主卡详情内展示。
7. 清理早期默认版本号导入残留，并为疫病生成独立关系分组。
8. 验证至少以下样例：
   - 普通搜索不出现 `*咕噜*`、英雄技能、token。
   - 英雄牌详情页能看到英雄技能。
   - 职业兆示牌详情页只看到对应职业兆示衍生物。
   - 中立兆示牌详情页能看到所有兆示衍生物。
   - `兵主之杖` 详情页能看到三张疫病牌，英文版本历史不再出现 `1`。
