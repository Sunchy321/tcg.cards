# Todo

- [x] 定义统一 desktop 配置结构与文件名
- [x] 定义设置页信息架构与游戏侧栏
- [x] 增加按游戏读写设置的 Tauri 命令
- [x] 重构设置页以承载 hearthstone 配置项
- [x] 调整 hsdata 数据源页为只读状态页
- [x] 保持导入页基于当前配置的 hsdata repo 工作
- [ ] 完成聚焦验证并清理旧接口与旧配置读写

# desktop 游戏设置与本地 repo 配置实施计划

## 步骤 1：收敛底层配置模型

- 将 `StoredHsdataSettings` 替换为统一 desktop 配置结构
- 定义按游戏组织的设置结构
- 将配置文件路径从 `hsdata-settings.json` 收敛到统一文件名

## 步骤 2：定义设置界面信息架构

- 扩展现有 `/settings` 页面，而不是新建零散配置页
- 左侧提供游戏导航
- 右侧展示当前游戏的设置分组与具体配置项

## 步骤 3：直接切换到新配置文件

- 只读取新的统一配置文件
- 删除旧的 hsdata 单仓库配置读写逻辑
- 接受已存在的旧本地配置需要人工重新录入

## 步骤 4：扩展 Tauri 命令边界

- 新增按游戏读取和写入设置的通用命令
- 保留 hsdata 状态读取、来源枚举、XML 读取命令基于当前 hsdata 配置工作
- 在前端彻底迁移后移除旧的 `hsdata_get_repo_path` / `hsdata_set_repo_path`

## 步骤 5：重构设置页与 hsdata 数据源页

- 在 `Hearthstone` 设置区中加入 hsdata 仓库路径配置项
- 数据源页移除路径输入区，只保留状态、来源列表和跳转设置按钮
- 保持 repo 校验错误与当前仓库状态分离展示

## 步骤 6：适配数据导入页

- 保持导入页读取当前 hsdata 配置的状态与来源
- 在未配置 repo 时明确提示缺少 hsdata 仓库，并引导前往设置页
- 确保现有来源选择逻辑在统一配置下仍然可用

## 步骤 7：验证与收口

- 验证设置页中配置、更新、清空 hsdata repo 的状态流转
- 验证当前配置 repo 下的来源枚举、读取、导入与投影
- 验证游戏侧栏切换与配置内容展示
- 清理旧配置结构和不再使用的前端调用
