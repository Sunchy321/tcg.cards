# Todo

- [x] 让搜索页读取本地样例卡，并按关键词匹配中文名、英文名、编号和效果文本。
- [x] 设计搜索结果卡片，命中样例卡时展示可点击结果。
- [x] 新增卡牌详情页路由 `/cards/16178681`。
- [x] 设计详情页的卡图、基础资料和中英文效果对照区域。
- [x] 运行 lint、build，并检查搜索页和详情页路由。

# 实施计划

## 1. 搜索页接入本地样例

在 `apps/site-yugioh/app/pages/search/index.vue` 中导入 `sampleCard`，基于当前 `q` 进行本地匹配。匹配字段包括中文名、英文名、日文名、编号、中文效果、英文效果。

## 2. 搜索结果卡片

替换当前「查询已记录」占位内容。命中时展示结果摘要和一张横向结果卡；未命中时展示无结果状态；没有查询时展示搜索建议。

## 3. 详情页路由

新增 `apps/site-yugioh/app/pages/cards/[id].vue`。当前只支持 `16178681`，不是该编号时展示轻量未找到状态。

## 4. 详情页布局

详情页使用真实卡图作为主视觉。桌面端使用卡图 + 档案 + 效果文本的分栏布局；移动端单列堆叠。卡图支持中文和英文切换。

## 5. 验证

运行 `bun --filter site-yugioh lint` 和 `bun --filter site-yugioh build`。启动本地站点后检查 `/search?q=异色眼灵摆龙`、`/search?q=Odd-Eyes`、`/search?q=16178681` 和 `/cards/16178681`。
