{
  "name": "tcg-cards",
  "license": "MIT",
  "dependencies": {
    "drizzle-orm": "^1.0.0-beta.20"
  },
  "devDependencies": {
    "@turbo/gen": "^2.8.12",
    "prettier": "^3.2.5",
    "turbo": "^2.6.1"
  },
  "packageManager": "bun@1.3.10",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "postinstall": "turbo run postinstall",
    "gen": "sh -c 'turbo gen new-site --args \"$1\" && bun install' sh"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
